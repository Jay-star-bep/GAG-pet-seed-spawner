-- VISUAL UI ONLY
-- Put this LocalScript inside StarterGui

local player = game.Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "PetReplicatorUI"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

-- Main window
local main = Instance.new("Frame")
main.Size = UDim2.new(0, 500, 0, 300)
main.Position = UDim2.new(0.5, -250, 0.5, -150)
main.BackgroundColor3 = Color3.fromRGB(23, 26, 39)
main.BorderSizePixel = 0
main.Parent = gui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 16)
mainCorner.Parent = main

-- Top bar
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1, 0, 0, 50)
topBar.BackgroundColor3 = Color3.fromRGB(30, 34, 50)
topBar.BorderSizePixel = 0
topBar.Parent = main

local topCorner = Instance.new("UICorner")
topCorner.CornerRadius = UDim.new(0, 16)
topCorner.Parent = topBar

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -100, 1, 0)
title.Position = UDim2.new(0, 20, 0, 0)
title.BackgroundTransparency = 1
title.Text = "Pet Replicator"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.TextXAlignment = Enum.TextXAlignment.Left
title.Font = Enum.Font.GothamBold
title.TextSize = 22
title.Parent = topBar

-- Minimize and Close symbols (visual only)
local minimize = Instance.new("TextLabel")
minimize.Size = UDim2.new(0, 30, 1, 0)
minimize.Position = UDim2.new(1, -70, 0, 0)
minimize.BackgroundTransparency = 1
minimize.Text = "-"
minimize.TextColor3 = Color3.fromRGB(200,200,200)
minimize.Font = Enum.Font.GothamBold
minimize.TextSize = 24
minimize.Parent = topBar

local close = Instance.new("TextLabel")
close.Size = UDim2.new(0, 30, 1, 0)
close.Position = UDim2.new(1, -40, 0, 0)
close.BackgroundTransparency = 1
close.Text = "X"
close.TextColor3 = Color3.fromRGB(200,200,200)
close.Font = Enum.Font.GothamBold
close.TextSize = 20
close.Parent = topBar

-- Pet display box
local petBox = Instance.new("Frame")
petBox.Size = UDim2.new(1, -40, 0, 120)
petBox.Position = UDim2.new(0, 20, 0, 80)
petBox.BackgroundColor3 = Color3.fromRGB(32, 36, 55)
petBox.BorderSizePixel = 0
petBox.Parent = main

local petCorner = Instance.new("UICorner")
petCorner.CornerRadius = UDim.new(0, 14)
petCorner.Parent = petBox

-- Pet info text
local petInfo = Instance.new("TextLabel")
petInfo.Size = UDim2.new(1, -20, 1, -20)
petInfo.Position = UDim2.new(0, 10, 0, 10)
petInfo.BackgroundTransparency = 1
petInfo.Text = "● Cryptid\nMega Neon • Fly • Ride"
petInfo.TextWrapped = true
petInfo.TextColor3 = Color3.fromRGB(210, 170, 255)
petInfo.TextXAlignment = Enum.TextXAlignment.Left
petInfo.TextYAlignment = Enum.TextYAlignment.Top
petInfo.Font = Enum.Font.GothamSemibold
petInfo.TextSize = 18
petInfo.Parent = petBox

-- Big center text
local spawning = Instance.new("TextLabel")
spawning.Size = UDim2.new(1, 0, 0, 60)
spawning.Position = UDim2.new(0, 0, 0.5, -20)
spawning.BackgroundTransparency = 1
spawning.Text = "Spawning"
spawning.TextColor3 = Color3.fromRGB(255,255,255)
spawning.Font = Enum.Font.GothamBold
spawning.TextSize = 40
spawning.Parent = main

-- Bottom button (visual only)
local button = Instance.new("Frame")
button.Size = UDim2.new(1, -40, 0, 55)
button.Position = UDim2.new(0, 20, 1, -75)
button.BackgroundColor3 = Color3.fromRGB(90, 120, 255)
button.BorderSizePixel = 0
button.Parent = main

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 14)
btnCorner.Parent = button

local btnText = Instance.new("TextLabel")
btnText.Size = UDim2.new(1, 0, 1, 0)
btnText.BackgroundTransparency = 1
btnText.Text = "REPLICATE PET ⚡"
btnText.TextColor3 = Color3.fromRGB(255,255,255)
btnText.Font = Enum.Font.GothamBold
btnText.TextSize = 20
btnText.Parent = button
